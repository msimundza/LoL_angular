'use strict';

describe('myApp.championsList module', function() {

  beforeEach(module('myApp.championsList'));

  describe('championsList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var championsListCtrl = $controller('championsListCtrl');
      expect(championsListCtrl).toBeDefined();
    }));

  });
});